{
  "name": "purescript-tauri",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx spago bundle-module && tsc && vite build",
    "preview": "vite preview",
    "tauri": "npx spago bundle-module && tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2",
    "esbuild": "^0.24.0",
    "purescript": "^0.15.15",
    "spago": "^0.21.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5.2.2",
    "vite": "^5.3.1"
  }
}
